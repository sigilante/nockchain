# Native parser notes

This directory is for notes that are expressly about the native parser as a
standalone parser.

If a parser behavior is documented because it changes native compiler artifacts,
source spots, `dbug` metadata, or parity with the canonical compiler, put that
note under `open/docs/native-compiler/` instead.

Primary parser crate:

- `hatch` at `open/crates/hatch/`
