name: Release Binaries

on:
  push:
    branches: ["master"]
  pull_request:
    branches: ["master"]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - os: macos-latest
            target: aarch64-apple-darwin
    permissions:
      contents: write  # Required for action-gh-release
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache Rust dependencies
        uses: swatinem/rust-cache@v2

      - name: Build hoon
        run: |
          cargo build --release --bin hoon --manifest-path crates/hoon/Cargo.toml
          file target/release/hoon

      - name: Build hoonc
        run: |
          cargo build --release --bin hoonc --manifest-path crates/hoonc/Cargo.toml
          file target/release/hoonc

      - name: Build nockup
        run: |
          cargo build --release --bin nockup --manifest-path crates/nockup/Cargo.toml
          file target/release/nockup

      - name: Package binaries
        run: |
          mkdir -p dist
          # Copy and make executable
          cp target/release/hoon dist/hoon
          chmod +x dist/hoon
          cp target/release/hoonc dist/hoonc
          chmod +x dist/hoonc
          cp target/release/nockup dist/nockup
          chmod +x dist/nockup
          ls
          # Create archives
          tar -czf dist/hoon-${{ github.ref_name }}-${{ matrix.target }}.tar.gz -C dist hoon
          ls
          tar -czf dist/hoonc-${{ github.ref_name }}-${{ matrix.target }}.tar.gz -C dist hoonc
          ls
          tar -czf dist/nockup-${{ github.ref_name }}-${{ matrix.target }}.tar.gz -C dist nockup
          ls

      - name: Upload release assets
        if: github.event_name == 'push' && github.ref == 'refs/heads/master'  # Only on push to master, not PRs
        uses: softprops/action-gh-release@v1
        with:
          files: |
            dist/hoon-${{ github.ref_name }}-${{ matrix.target }}.tar.gz
            dist/hoonc-${{ github.ref_name }}-${{ matrix.target }}.tar.gz
            dist/nockup-${{ github.ref_name }}-${{ matrix.target }}.tar.gz