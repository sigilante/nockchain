::  wet gate whose formal sample is a fork of two differently-faced atoms:
::  ++redo's reference reduction keeps both face stacks and ++dear cannot
::  unify them (redo-match).
|%
++  wig
  |*  a=?(p=@ q=@)
  1
++  main
  |=  x=@ud
  (wig x)
--
